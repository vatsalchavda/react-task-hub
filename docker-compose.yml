version: '3.8'

services:
  app:
    build: .
    ports:
      - "0.0.0.0:3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - REACT_APP_ENABLE_LAZY_LOADING=false
      - REACT_APP_LAZY_LOADING_DELAY=3000
    command: npm run dev

